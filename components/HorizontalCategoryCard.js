import { StyleSheet, Text, View } from "react-native";

export default function HorizontalCategoryCard(props) {
  return (
    <View style={styles.container}>
      {/* Image
      Park name
      address
      Distance */}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "white",
    alignItems: "center",
    justifyContent: "center",
  },
});
